Project showing the use of mongdb for executing checmical compound similarity algorithms.

Make sure MongoDB is running when executing the project code. Also, use a version that has the aggregation framework included.